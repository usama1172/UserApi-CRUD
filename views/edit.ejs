<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-5 text-blue-300">
            <a href="/read">Read All Users</a>
        </div>
        <h1 class="text-2xl font-bold mb-2">Edit User</h1>
        <form action="/update/<%= user._id %>" method="post">
            <input class="px-5 py-3 bg-transparent border-zinc-800 border-2 rounded" type="text" name="name" placeholder="Name" value="<%= user.name %>"/>
            <input class="px-5 py-3 bg-transparent border-zinc-800 border-2 rounded" type="email" name="email" placeholder="Email" value="<%= user.email %>"/>
            <input class="px-5 py-3 bg-transparent border-zinc-800 border-2 rounded" type="text" name="image" placeholder="ImageUrl" value="<%= user.image %>"/>
            <input class="ml-3 bg-yellow-500 py-2 px-5 rounded-lg" type="submit" value="update user"/>
        </form>
    </div>
</body>
</html>